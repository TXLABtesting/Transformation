# حزمة staging — 2026-08-22

هذه أحدث نسخة من الكود جاهزة للنشر على بيئة staging. البنية والترحيلات
**هي نفسها المعتمدة** (0001 → 0013 — لا ترحيلات جديدة). المطلوب من فريق
IT ثلاثة أمور فقط: ربط قاعدة البيانات، النشر، وربط الدخول الموحّد.
التفاصيل الكاملة في `GO-LIVE.md` و`DEPLOYMENT.md`، وكل متغيرات البيئة
مشروحة داخل `.env.example`.

## 1) قاعدة البيانات (PostgreSQL 16)

```bash
cp .env.example .env            # ثم تعبئة القيم — أهمها DATABASE_URL
npm ci
npx prisma migrate deploy       # 0001 → 0013 (آمنة على قاعدة staging القائمة)
npm run db:seed                 # مرجعية فقط — idempotent
```

- على قاعدة staging القائمة: راجع ملاحظة إعادة ترقيم ترحيلَي التحصين
  وتنظيف دور `stream_deputy` في `GO-LIVE.md` §1.
- لا تضبط `SEED_DEMO_ITEMS` — تبقى المحفظة فارغة.

## 2) النشر

```bash
npm run build && npm start      # أو Docker كما في DEPLOYMENT.md §2
```

متغيرات إلزامية قبل البناء (تُدمج في حزمة العميل):

- `NEXT_PUBLIC_DATA_MODE=api`
- `NEXT_PUBLIC_DEMO_MODE=0` و`NEXT_PUBLIC_DEMO_DATA=0` — **يجب أن تبقى 0**
  (الـ 1 يُظهر تبويب تبديل الأدوار وقائمة تبديل الجهة — للعرض التجريبي فقط).
- `SESSION_SECRET` (openssl rand -hex 32) و`STATE_API_TOKEN`.

اختياري: متغيرات `SMTP_*` لتفعيل بريد استفسارات «تواصل معنا» لكل مسار
(بدونها يبقى كل شيء يعمل والاستفسارات تظهر للمشرف في اللوحة).

## 3) الدخول الموحّد (SSO)

- `AUTH_PROVIDER` و`NEXT_PUBLIC_AUTH_PROVIDER`: `uaepass` أو
  `workspaceone` (قيمة `mock` معطّلة في الإنتاج).
- UAE PASS: `NEXT_PUBLIC_UAEPASS_MODE=live` + `UAEPASS_CLIENT_ID` /
  `UAEPASS_CLIENT_SECRET` / `UAEPASS_REDIRECT_URI`
  (`https://YOUR_HOST/api/auth/uaepass/callback`) و`UAEPASS_ENV`.
- Workspace ONE / OIDC عام: متغيرات `OIDC_*` في `.env.example`
  (`OIDC_REDIRECT_URI=https://YOUR_HOST/callback`).
- `BOOTSTRAP_ADMIN_EMAILS`: بريد أول مشرف نظام — يُمنح `system_admin`
  تلقائياً عند أول دخول، وبقية الحسابات تُدار من لوحة المشرف.
- بعد الدخول يعود المستخدم إلى الصفحة الرئيسية `/` ويظهر له زر
  «منصة الإدخال» الموجّه حسب جهته (`/dashboard` أو `/moca`).

## أبرز ما استجد في هذه الحزمة منذ حزمة 2026-08-18

- الموقع العام الجديد (`/`، `/about`، `/library`، `/contact`، `/login`)
  مع CMS كامل في لوحة المشرف — بلا أي تغيير على قاعدة البيانات
  (`GO-LIVE.md` §6).
- استفسارات «تواصل معنا»: الاستقبال عام، الاطلاع **للمشرف فقط**،
  والفرق تستقبل بالبريد لكل مسار (`GO-LIVE.md` §7) — اعتمادية جديدة
  واحدة `nodemailer` تُثبّت مع `npm ci`.
- نموذج حصر العمليات الرسمي معتمداً في الإدخال اليدوي والرفع بالملف،
  ودفعات الإطلاق السبع المعتمدة بتواريخها في نسخة `/moca`.
