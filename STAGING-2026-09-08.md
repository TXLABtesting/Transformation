# حزمة staging — 2026-09-08 (الإصدار v14)

هذه أحدث نسخة من الكود، جاهزة للنشر على staging ثم الإنتاج. كل ما يلزم فريق
تقنية المعلومات موجود هنا خطوةً خطوة؛ التفاصيل المرجعية في `GO-LIVE.md`
و`DEPLOYMENT.md`، ومتغيرات البيئة كلها مشروحة داخل `.env.example`.

**الخلاصة:** لا تغيير في متغيرات البيئة ولا في طريقة النشر. الجديد على
القاعدة خمسة ترحيلات إضافية فقط (`0015` → `0019`) كلها **إضافة** لا تمس أي
جدول قائم، وجدولان جديدان يملأهما IT لعمود «اسم مساعد الذكاء الاصطناعي»
(§2). كل ما عدا ذلك تغييرات في الواجهة والمنطق تصل مع البناء.

---

## 1) قاعدة البيانات (PostgreSQL 16)

```bash
cp .env.example .env            # ثم تعبئة القيم — أهمها DATABASE_URL
npm ci
npx prisma migrate deploy       # يطبّق ما لم يُطبَّق بعد حتى 0019 — آمن على قاعدة staging القائمة
npm run db:seed                 # مرجعية فقط — idempotent (جهات، مسارات، دليل الخدمات)
```

الترحيلات منذ حزمة 2026-08-24 (كلها `CREATE TABLE IF NOT EXISTS` / إضافة أعمدة):

| الترحيل | ما يفعله | مرجعه |
|---|---|---|
| `0015_moca_and_strategic_projects` | جداول نسخة الوزارة والمشاريع الاستراتيجية | `GO-LIVE.md` §10 |
| `0016_proj_member_identity` | هوية أعضاء المشاريع (بريد/هاتف) | `GO-LIVE.md` §١٢ |
| `0017_ai_assistants` | جدولا `ai_assistants` و`item_activity_assistants` | §2 أدناه |
| `0018_activity_assistant_not_null` | `activity_id` نص غير فارغ (`''` = المدخل كله) ليعمل قيد التفرد | §2 |
| `0019_assistant_link_to_state_items` | إزالة المفتاح الأجنبي إلى جدول `items` + فهرس على `item_id` | §2 |

- على قاعدة staging القائمة: `prisma migrate deploy` يكتشف المطبَّق ويكمل الباقي.
- لا تضبط `SEED_DEMO_ITEMS` — تبقى المحفظة فارغة.

## 2) عمود «اسم مساعد الذكاء الاصطناعي» — يُملأ من القاعدة

العمود ظاهر في قوائم الحصر (فارغ «—» الآن) ويُقرأ **حصراً** من القاعدة عند
كل تحميل عبر `GET /api/state`؛ لا يُدخله المستخدم، ولا نسخة منه داخل كتلة
الحالة. التعبئة والتعديل من جانب IT بـ SQL (أو من واجهة إدارة تُبنى لاحقاً).

**البنية**

| الجدول | الأعمدة |
|---|---|
| `ai_assistants` | `id` (نص، مفتاح)، `name` (الاسم العربي — هو ما يُعرض)، `name_en`، `desc`، `vendor`، `status` (`لم يبدأ بعد` / `قيد التنفيذ` / `مكتمل` / `متوقف مؤقتاً`)، `stream_id`، `entity_id`، `created_at`، `updated_at` |
| `item_activity_assistants` | `id`، `item_id`، `activity_id`، `assistant_id`، `created_at` — قيد تفرد على `(item_id, activity_id, assistant_id)` |

**المعرّفات**

- `item_id` = معرّف المدخل داخل كتلة الحالة: `app_state.data->'items'[]->>'id'`
  (مدخلات المنصة تُحفظ في الكتلة لا في جدول `items` — لذلك أُزيل المفتاح
  الأجنبي في `0019`).
- `activity_id` = معرّف العملية الفرعية داخل المدخل `activities[]->>'id'`،
  أو `''` (نص فارغ) = المدخل كله بكل عملياته الفرعية.
- علاقة متعددة إلى متعددة: للعملية الفرعية مساعد أو أكثر، وللمساعد عملية
  فرعية أو أكثر ولو في مدخلات مختلفة — صف لكل زوج.
- حذف مساعد يحذف روابطه تلقائياً (`ON DELETE CASCADE`)؛ حذف الرابط يُزيل
  الاسم من المدخل في التحميل التالي مباشرة.
- مدخل قديم بلا `activities[].id` (أُنشئ قبل حزمة 7 سبتمبر): يُربط على مستوى
  المدخل (`''`)، أو يُعاد حفظه من الواجهة مرة واحدة فتُسند المعرّفات.

**الملف الجاهز:** `scripts/assistants-example.sql` — استعلام يعرض المدخلات
وعملياتها الفرعية بمعرّفاتها، ثم أمثلة الإدراج والربط والمراجعة والحذف.

**التحقق** (نُفِّذ على قاعدة نظيفة بالكود نفسه): مساعد على مستوى عملية فرعية
+ مساعد على مستوى المدخل + مدخل ثانٍ، ظهور الاسمين في العمود، رفض التكرار،
وزوال الاسم فور الحذف — 9/9.

## 3) النشر والبيئة — بلا تغيير

```bash
npm run build && npm start      # أو Docker كما في DEPLOYMENT.md §2
```

إلزامي قبل البناء (يُدمج في حزمة العميل):

- `NEXT_PUBLIC_DATA_MODE=api`
- `NEXT_PUBLIC_DEMO_MODE=0` و`NEXT_PUBLIC_DEMO_DATA=0` — **يجب أن تبقيا 0**
  (بهما يختفي مبدّل الأدوار وقائمة تبديل الجهة والبيانات التجريبية تماماً).
- `SESSION_SECRET` و`STATE_API_TOKEN`، ومتغيرات SSO (`GO-LIVE.md` §1 و`.env.example`).
- `BOOTSTRAP_ADMIN_EMAILS`: بريد أول مشرف نظام.

الأدوار والنطاقات كلها من القاعدة (`GO-LIVE.md` §١١): الدور يحدد اللوحة
والمسار والجهة — لا اختيار يدوي في الواجهة.

## 4) ما الجديد وظيفياً منذ حزمة 2026-08-24

**أ. مسار العمليات**
- سؤال جديد في الإدخال وقالب الملف: «هل سيتم تحويل العملية؟» (نعم/لا) —
  إلزامي؛ «لا» يغلق أولوية التحول ومخاطره وفترته.
- أعمدة «أولوية التحول» و«مخاطر التحول» و«فترة التحويل» تُقرأ من ملف الجهة
  إن وُجدت بأي صياغة قريبة، وتُستكمل داخل المنصة إن غابت. القالب الرسمي 19 عموداً.
- عمود «اسم مساعد الذكاء الاصطناعي» (§2) بدل «نوع عملية الدعم المؤسسي».

**ب. فترة التحويل لكل المسارات (العمليات، الاستراتيجية، الخدمات)**
- قائمة «فترة التحويل» في الإدخال والقالب = فترات دفعات الإطلاق المعتمدة
  نفسها («سبتمبر – نوفمبر 2026»…)، وتُسند عند الاستيراد إلى الدفعة وأول شهر فيها.
- الفترة **اختيارية**: غيابها لا يمنع الإرسال لكنه يُعلَّم «فترة التحويل غير
  محددة» في المراجعة والنموذج والقائمة، ويظهر المدخل في بطاقة «بلا فترة
  تحويل — للتحديد» في صفحة الدفعات.
- صفحة الدفعات: منسق الجهة يحدد الفترة أو يغيّرها من الصف مباشرة **حتى
  اعتماد المدخل**؛ بعد الاعتماد تُقفل الدفعة («مقفل بعد الاعتماد»).
- اعتماد واحد فقط: لا «إرسال للاعتماد» ثانٍ من صفحة الدفعات — الاعتماد يتم
  مرة واحدة عند وصول المدخل إلى فريق المسار.

**ج. رفع فريق المسار بالنيابة عن جهة**
- المرفوع بالنيابة يصل **منسق الجهة** مسودةً بعلامة «مرفوعة بالنيابة —
  بانتظار تأكيدكم» مع إشعار، ويستطيع تحديد الكل و«تأكيد».
- تأكيد كما رُفع → **معتمد** مباشرة ويظهر للجميع. تعديل ثم تأكيد → يذهب
  لاعتماد فريق المسار. الناقص لا يُؤكَّد حتى يُستكمل (الرسالة تعدّه).
- فريق المسار لا يعتمد المرفوع بالنيابة قبل تأكيد الجهة.

**د. محرك الاستيراد (Excel)**
- مطابقة الأعمدة على ثلاث مراحل: حرفية ← بعد التطبيع (همزات/تاء مربوطة/ترقيم/
  «المساعد»/«العمليات») ← تقريبية منضبطة بأطول تطابق؛ العنوان الغامض لا
  يُنسب لأي حقل، وأعمدة الاسم لا تُطابق بالاحتواء («وصف المهمة» ≠ «المهمة»).
- تطبيع القيم إلى خيارات **المسار نفسه** (عالي/عالية/مرتفع، غير قابل/غير
  قابل للتحول، نعم/لا/جزئياً…) مع قبول الفروق الإملائية.
- الفترة: «من – إلى» بأي صيغة (عربي/شامي/إنجليزي/رقمي/أرقام عربية-هندية) →
  أول شهر ← دفعته؛ اسم الدفعة وحده → أول شهر فيها؛ نص غير مفهوم → فارغ ومُعلَّم.
- الأعمدة الزائدة تُتجاهل، والملف لا يُرفض لأجلها. رفع الفريق يبقى مسودة دائماً.

**هـ. مرشحات القوائم** موحّدة مع خيارات نماذج الإدخال لكل مسار (قابلية
التحول، هل سيتم التحويل، أولوية التحول، الجهة…).

**و. نصوص:** «وكيل» → «مساعد» في كل المنصة. **وسائط:** فيديو الواجهة من
المصدر عالي الجودة (H.264 1080p) مع رقم إصدار في روابط الأصول لتجاوز
التخزين المؤقت؛ صور الموقع العام تُدار من القاعدة (`0014`) والملفات في
`public/` قيم افتراضية فقط.

ما بين 25 أغسطس و3 سبتمبر موثّق في `GO-LIVE.md` §6 → §١٥ (الموقع العام،
الوسائط، معالجة فحص TDRA، المشاريع الاستراتيجية ونسخة الوزارة، الأدوار من
القاعدة، النطاقات المتعددة، تصحيحات المشرف والوزارة).

## 5) ما ليس في هذه الحزمة (خاص بالنسخة التجريبية فقط)

سوق المساعدين (`/marketplace`)، مساري «بناء القدرات» و«التقنيات» في لوحة
الإدخال، مبدّل الأدوار والجهات، البيانات المبذورة، الصور المكبَّرة. نسخة IT
ثلاثة مسارات إدخال (العمليات، الاستراتيجية، الخدمات) والدور من القاعدة.

## 6) التحقق المنفَّذ (2026-09-08)

| الفحص | النتيجة |
|---|---|
| `prisma migrate deploy` حتى `0019` على PostgreSQL 16 | ✅ |
| `npm run build` (نسخة الخادم بكل مسارات API، بلا أعلام تجريبية) | ✅ صفر أخطاء |
| رفع ملف بأعمدة الجهة الإضافية (أولوية/مخاطر/فترة/هل سيتم) + القيم المطبَّعة | ✅ 12/12 |
| رفع بالنيابة → تأكيد المنسق (كما هو → معتمد؛ معدَّل → فريق المسار؛ الناقص يُستكمل) | ✅ 15/15 |
| صفحة الدفعات: تحديد/تغيير الفترة، القفل بعد الاعتماد، بطاقة بلا فترة | ✅ 9/9 |
| المرشحات لكل مسار = خيارات الإدخال | ✅ 30/30 |
| عمود المساعد من القاعدة (§2) | ✅ 9/9 |
| واجهات الجلسة والنطاقات والأدوار والوزارة والمشاريع (بطارية الانحدار، 13 مجموعة) | ✅ 165 فحصاً |
| الاستجابة على الجوال/اللوحي/المكتب (المنصة والوزارة) | ✅ 38/38 |

## 7) قائمة قبول مقترحة لفريق IT بعد النشر

1. دخول مشرف النظام (`BOOTSTRAP_ADMIN_EMAILS`) → لوحة الإدارة → إنشاء منسق
   جهة اتحادية وعضو فريق مسار العمليات (`GO-LIVE.md` §١١-٢).
2. بحساب فريق المسار: «رفع ملف» بالنيابة عن الجهة بقالب العمليات الرسمي
   (زر «تنزيل القالب») مع تعبئة «هل سيتم تحويل العملية؟» و«فترة التحويل»
   من قائمتيهما → يظهر للمنسق «مرفوعة بالنيابة — بانتظار تأكيدكم».
3. بحساب المنسق: تحديد الكل → «تأكيد» → الحالة «معتمد»؛ ثم تعديل مدخل آخر
   وتأكيده → «بانتظار اعتماد فريق المسار».
4. صفحة الدفعات: مدخل بلا فترة يظهر في «بلا فترة تحويل — للتحديد»، تحديد
   فترته من الصف، ثم بعد اعتماده تصبح القائمة «مقفل بعد الاعتماد».
5. تنفيذ `scripts/assistants-example.sql` (الاستعلام 1 لاختيار المعرّفات ثم
   الإدراج) → يظهر الاسم في عمود «اسم مساعد الذكاء الاصطناعي» فور التحديث.

> **تذكير تشغيلي**: `NEXT_PUBLIC_DEMO_MODE` و`NEXT_PUBLIC_DEMO_DATA` يجب
> أن تبقيا `0` في كل بناء لـ staging والإنتاج.
