# AIGP — حزمة الإطلاق النهائية (Go-Live)

**الفرع:** `it-after-changes-2` · **تاريخ الإصدار:** 2026-08-22

هذه هي الحزمة النهائية للنشر على البيئة الحية. بنية قاعدة البيانات **هي
نفسها المعتمدة سابقاً دون أي تغيير** — لا ترحيلات جديدة منذ حزمة staging.
خطوات النشر التفصيلية في `DEPLOYMENT.md`، والأدوار والصلاحيات في
`docs/roles-pages-access.md`، وسجل التغييرات الكامل في
`docs/CHANGES-2026-08-11.md` بملاحقه.

---

## 1. قاعدة البيانات — البنية نفسها المعتمدة سابقاً

مجموعة الترحيلات: **`0001 … 0011`** كما هي، يليها ترحيلا التحصين اللذان
أضافهما فريق IT في نسخة v4 بعد **إعادة ترقيمهما**:

- `0006_rbac_security` ← صار **`0012_rbac_hardening_v3`**
- `0007_role_assignment_rules` ← صار **`0013_role_assignment_rules`**

> **سبب إعادة الترقيم:** بترقيمهما القديم كانا يسبقان
> `0007_roles_sessions_notifications` فيفشل الترتيب على قاعدة بيانات
> جديدة (`relation "roles" already exists`). محتواهما لم يتغيّر (جُعلت
> قيود FK فيهما idempotent فقط) — وعلى قاعدة staging القائمة التي طُبّقا
> عليها بالاسمين القديمين سيعيد `migrate deploy` تشغيلهما بالاسمين
> الجديدين بأمان (كلاهما idempotent) مع تحذيرٍ عن الاسمين القديمين
> المسجّلين في `_prisma_migrations` يمكن تجاهله.

```bash
npx prisma migrate deploy              # 0001 → 0013
npm run db:seed                        # مرجعية — idempotent
```

> **لا يوجد دور نائب.** أُزيل ما أضافته نسخة v4 من دور `stream_deputy`
> (السكربت والربط في الكود). إن كان الدور قد أُنشئ على قاعدة staging،
> يُنظَّف هكذا — تُنقل حساباته إن وُجدت إلى `stream_owner` ثم يُحذف:
>
> ```sql
> UPDATE user_roles SET role_id = (SELECT id FROM roles WHERE code='stream_owner')
>  WHERE role_id = (SELECT id FROM roles WHERE code='stream_deputy')
>    AND NOT EXISTS (SELECT 1 FROM user_roles ur2
>                     WHERE ur2.user_id = user_roles.user_id
>                       AND ur2.role_id = (SELECT id FROM roles WHERE code='stream_owner'));
> DELETE FROM user_roles       WHERE role_id = (SELECT id FROM roles WHERE code='stream_deputy');
> DELETE FROM role_permissions WHERE role_id = (SELECT id FROM roles WHERE code='stream_deputy');
> DELETE FROM roles            WHERE code = 'stream_deputy';
> ```

الحقول التي أُضيفت بعد `0011` كلها داخل العمود القائم `items.activities`
(jsonb) ولا تحتاج ترحيلاً:

| داخل كل نشاط في `activities` | القيم | الغرض |
|---|---|---|
| `batchWf` | `draft` \| `pending` \| `approved` | حالة اعتماد التوزيع على الدفعة |
| `batchRet` | `info` \| `reject` | نوع قرار الإعادة/الرفض على التوزيع |
| `batchNote` | نص | سبب الإعادة/الرفض — يظهر للمنسق |

قواعد يطبّقها الخادم (تفصيلها في ملحق 2026-08-14 من وثيقة التغييرات):

1. دورتا اعتماد منفصلتان: اعتماد محتوى المدخل (كما هو) واعتماد التوزيع
   على الدفعات (جديدة). لا تمسّ إحداهما حالة الأخرى.
2. لا يُوزَّع على دفعات الإطلاق إلا مدخل معتمد (`exec/launch/done`) —
   غير ذلك يُرفض 409.
3. التوزيع `pending` أو `approved` مقفل: نقل أو إزالة أو تعديل تواريخ → 409.
4. `POST /api/items/batch-drafts/:streamId/submit` يرسل التوزيعات
   بجسم `{ keys?: ["<itemId>::<actIdx>"] }` — كلها عند الإغفال.
5. قرار التوزيع: `approve` يقفل؛ `info`/`reject` يعيدانه `draft` مع
   `batchRet`/`batchNote`.

### دمج نسخة IT (aigp-v4-portal)

هذه الحزمة مبنية على نسخة `aigp-v4-portal` التي عدّلها فريق IT، مضافاً
إليها كل تغييرات المنصة اللاحقة. ما بقي من نسخة IT كما هو: التحصين
الأمني (V3)، لوحة الإدارة المتصلة بـ `/api/admin/*`، تشغيل الترحيلات
عند الإقلاع (Docker/K8s)، بريد المنسقين المعتمد، ومسار الدخول
`/api/auth/login`. التعديل الوحيد على عملهم: إعادة ترقيم الترحيلين
أعلاه، وإزالة دور `stream_deputy` الذي كانت النسخة قد أضافته — لا وجود
لدور نائب في البنية المعتمدة (أربعة أدوار).

## 2. الأدوار — البنية المعتمدة (أربعة أدوار)

| دور الواجهة | الاسم | رمز RBAC | النطاق |
|---|---|---|---|
| `coord` | منسق المسار في الجهة الاتحادية | `entity_coordinator` | جهته + مساره (يشمل المسودات) |
| `path` | فريق عمل المسار في المشروع | `stream_owner` | مساره عبر كل الجهات — معتمِد الدورتين |
| `ai` | اللجنة الوطنية للذكاء الاصطناعي المساعد | `ai_committee` | وطني — المعتمد فقط (اطلاع) |
| `admin` | مشرف النظام | `system_admin` | النظام بالكامل |

- الدوران الملغيان (`deputy`، `secretariat`) لم يكن لهما وجود في جدول
  `roles` — الدمج لا يغيّر أي صلاحية. الحسابات المحفوظة بهما تُرحَّل
  تلقائياً في الواجهة، وللتنظيف المباشر:

```sql
UPDATE users SET role = 'path' WHERE role = 'deputy';
UPDATE users SET role = 'ai'   WHERE role = 'secretariat';
```

- إعادة تشغيل البذر تحدّث مسميات `roles.name_ar` للأدوار الثلاثة
  المعاد تسميتها (idempotent).
- الأدوار الخلفية الأخرى (`entity_representative`، `entity_admin`،
  `viewer`، `auditor`، `program_admin`) تبقى مبذورة للتوافق ولا تملك
  صلاحيات اعتماد.

## 3. نسخة وزارة شؤون مجلس الوزراء

مسار مستقل `/moca` (حصر المهام والعمليات — 19 حقلاً مطابقة للنموذج
المعتمد، 9 جهات/مكاتب بقطاعاتها، الاعتماد مباشرة للجنة الوطنية).
وحدة معزولة تماماً عن منصة الجهات — تخزينها حالياً في المتصفح؛ إن
أريد ربطها بقاعدة البيانات فذلك ترحيل مستقل (`0012`) يُتفق عليه لاحقاً
ولا يمسّ البنية الحالية.

## 4. نتائج التحقق النهائي قبل التسليم (على PostgreSQL 16 نظيف)

| الفحص | النتيجة |
|---|---|
| `prisma migrate deploy` على قاعدة فارغة | ✅ 11 ترحيلاً طُبّقت |
| `npm run db:seed` | ✅ نجح |
| جدول `roles` بعد البذر | ✅ المسميات الأربعة المعتمدة |
| `service_catalog` | ✅ 1855 صفاً · 47 جهة |
| `items.activities` | ✅ jsonb |
| `npm run build` (نسخة الخادم الكاملة بكل مسارات API) | ✅ صفر أخطاء |

## 5. أبرز ما يتضمنه هذا الإصدار منذ حزمة staging السابقة

- دورة اعتماد التوزيع على الدفعات كاملةً (إرسال الكل أو توزيعاً واحداً،
  قرار فريق العمل بسبب إلزامي، قفل المعتمد) وعموداً حالة منفصلان.
- قصر التوزيع على المدخلات المعتمدة.
- إشعارات مصحّحة لكل دور (توزيعات معلّقة لفريق العمل تزول بالبتّ،
  وقرارات التوزيع بأسبابها للمنسق).
- صفحة وزارة شؤون مجلس الوزراء `/moca` بتصميم المنصة نفسه.
- تقليص الأدوار إلى البنية المعتمدة، الرفض حالة مستقلة «تم الرفض»،
  الإزالة لكل غير معتمد، دليل الخدمات مقيّد بالجهة، وكل تحسينات الواجهة
  الموثقة في سجل التغييرات.

## 6. تحديث ما بعد الحزمة: الموقع العام الجديد وبنية المسارات (بلا أي تغيير على قاعدة البيانات)

**المسارات الجديدة** (كلها صفحات عميل ثابتة تُبنى مع التطبيق نفسه):

| المسار | الصفحة |
|---|---|
| `/` | الصفحة الرئيسية العامة (فيديو الواجهة، المستهدفات، الأخبار، المسارات، البرنامج الزمني) |
| `/about` | من نحن (مسيرة التحول، اقتباس القيادة، المبادئ) |
| `/library` | المنشورات (تُغذّى من «وثائق المنشورات» في لوحة المشرف) |
| `/contact` | تواصل معنا (يصب في صندوق استفسارات المسار كما كان) |
| `/login` | صفحة تسجيل الدخول — زر UAE PASS يستدعي `/api/auth/login` كما هو |
| `/dashboard` | لوحات المنصة كما كانت على `/` سابقاً — بلا أي تغيير وظيفي |
| `/moca` | نسخة الوزارة كما هي |

**تدفق الدخول**: الزائر يرى الرئيسية ومن نحن فقط؛ بعد OIDC يعود المستخدم
إلى `/` (إعادة توجيه callback الحالية نفسها) ويظهر شريط الأعضاء وزر
«منصة الإدخال» الذي يوجّه مستخدم وزارة شؤون مجلس الوزراء إلى `/moca`
وبقية الجهات إلى `/dashboard`.

**تغييران فقط على طبقة الخادم — ولا ترحيلات جديدة:**

1. **`GET /api/site-content` (جديد، قراءة عامة بلا جلسة)**: يعيد الجزء
   العام فقط من كتلة `app_state` (محتوى الموقع الذي يحرره المشرف +
   وثائق المنشورات + النص التعريفي). لا يتسرب منه أي من بيانات المسارات
   أو الجهات أو المستخدمين، والكتابة تبقى حصراً عبر `PUT /api/state`
   بصلاحياتها الحالية (جلسة + نطاق شامل + رمز `STATE_API_TOKEN` إن ضُبط).
2. **رفع سقف `PUT /api/state` من 2MB إلى 10MB**: كتلة الحالة أصبحت تحمل
   محتوى الموقع العام بصوره المضغوطة (أخبار/محطات/أغلفة كـ data URLs).

**لوحة المشرف**: تبويب «الموقع العام» أصبح CMS كاملاً للصفحات الجديدة
(العناوين، فيديو الواجهة بالرابط، المستهدفات، الأخبار، المسارات، المراحل،
محطات مسيرة التحول بصورها، الاقتباس، المبادئ، نص تواصل معنا).
ملاحظة تشغيلية: الفيديوهات والملفات الكبيرة يُفضّل استضافتها كروابط
(الحقل المخصص في اللوحة) لا كملفات مضمّنة.

**الأصول**: أُضيفت أصول الموقع العام تحت `public/assets/web` (~13MB —
صور وفيديو الواجهة). لا تغيير على `package.json` ولا على Docker ولا على
متغيرات البيئة ولا على الترحيلات (تبقى 0001–0013 كما وثّق أعلاه).

## 7. استفسارات «تواصل معنا» — لوحة المشرف فقط والبريد لكل مسار

- **`POST /api/contact` (عام بلا جلسة)**: يسجّل استفسار الزائر في كتلة
  الحالة (`app_state` — بلا ترحيل جديد) ثم يرسله بالبريد إلى عنوان
  المسار المعيّن في لوحة المشرف.
- **الاطلاع للمشرف فقط**: قائمة الاستفسارات تظهر حصراً في لوحة المشرف
  («التواصل والاستفسارات» — ربط بريد كل مسار + الاستفسارات الواردة
  بحالة المعالجة). فرق المسارات واللجنة الوطنية **تستقبل الاستفسارات
  بالبريد فقط** ولا صندوق لها في اللوحات.
- **`GET /api/contact`** و**`PATCH`** (تبديل «تمت المعالجة») مقيّدان
  بجلسة مصادَق عليها بنطاق شامل (`system_admin`) — 401 بلا جلسة و403
  لغير النطاق الشامل؛ و`PUT /api/state` يدمج الاستفسارات بالاتحاد حتى
  لا تُمسح الاستفسارات الواصلة بين مزامنتين.
- **البريد اختياري التفعيل**: يُرسل فقط عند ضبط متغيرات البيئة —
  `SMTP_HOST` (إلزامي للتفعيل) و`SMTP_PORT` (افتراضي 587) و`SMTP_USER`/
  `SMTP_PASS` و`SMTP_FROM` و`SMTP_SECURE=1` للاتصال المشفّر المباشر.
  بلا تهيئة SMTP يبقى كل شيء يعمل (التسجيل والصناديق) وتُتجاهل خطوة
  البريد. ربط عنوان كل مسار من لوحة المشرف ← «التواصل والاستفسارات».
- **اعتمادية جديدة واحدة**: `nodemailer` (في `package.json`) — تُثبّت مع
  `npm ci` كالمعتاد ولا تتطلب أي إعداد إضافي.

## 8. مخزن وسائط الموقع العام — ترحيل جديد واحد (`0014_site_media`)

لتمكين المشرف من رفع صور وفيديو الصفحات العامة **كمرفقات** بجودتها الكاملة
(بدل روابط خارجية أو صور مضغوطة داخل كتلة الحالة):

- **ترحيل جديد**: `0014_site_media` — جدول مستقل `site_media` لا يمسّ أي
  جدول قائم؛ يُطبَّق بـ `prisma migrate deploy` كالمعتاد (0001 → 0014).
- **`POST /api/media`** (مشرف النظام فقط): يستقبل الملف كما هو — صور حتى
  15MB وفيديو mp4/webm حتى 200MB — ويعيد رابط `/api/media/:id`.
- **`GET /api/media/:id`** (عام): يخدم الملف بنوعه الصحيح مع
  `Cache-Control: public, max-age=31536000, immutable` و`ETag` ودعم
  `Range` لتشغيل الفيديو والتنقل فيه. الاستبدال يُنشئ معرّفاً جديداً،
  فالتخزين المؤقت الدائم آمن ولا يتحمّل الخادم إلا أول طلب من كل متصفح.
- **`DELETE /api/media/:id`** (مشرف النظام): تستدعيه اللوحة تلقائياً عند
  استبدال ملف أو الاستغناء عنه — فلا تتراكم ملفات مهجورة.
- كتلة الحالة تخزّن الروابط فقط (نصوص قصيرة) — لا أثر للوسائط على حجمها
  ولا على سرعة `/api/site-content`.

## 9. معالجة نتائج فحص TDRA/Checkmarx (تقرير CA_26_76 — 2026-08-26)

نتيجتان فقط في التقرير (لا حرجة ولا عالية)، وكلتاهما عولجتا في الكود:

- **Missing HSTS Header (متوسطة)**: كانت الترويسة مضبوطة أصلاً عبر
  `headers()` في `next.config.mjs` على كل المسارات، لكن الفاحص الساكن لا
  يتعرّف على هذا الشكل. أُضيف `middleware.ts` يضبط
  `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`
  برمجياً على **جميع** الاستجابات (صفحات، API، ملفات ثابتة) — تم التحقق
  بالفحص المباشر على الخادم. لا تغيير على Docker أو خطوات النشر؛ الملف
  يُلتقط تلقائياً في `next build`.
  **ملاحظة للنشر**: إن كان أمام التطبيق مُوزِّع/Proxy يُنهي TLS فتأكدوا
  ألا يحذف الترويسة، وأن يكون الوصول عبر HTTPS دائماً.
- **Unsafe Use Of Target blank (منخفضة)** في
  `components/site/PublicLibrary.tsx`: أصبح فتح النوافذ بالنمط الموصى به
  (`win.opener = null` قبل أي استخدام)، وأضيف `noopener` لكل روابط
  `target="_blank"` المتبقية.
- **Cache-Control (ملاحظة متابعة)**: أصبح `middleware.ts` يرسل
  `Cache-Control: no-store, no-cache, must-revalidate` و`Pragma: no-cache`
  على **كل** استجابات API الديناميكية (الجلسات وبيانات المستخدمين واللوحات)،
  فلا يخزّنها متصفح ولا وسيط. التخزين العام يبقى محصوراً بموردين غير
  حساسين مُهيّأين بإصدارات: `/api/media/:id` (معرّف جديد عند كل استبدال —
  `immutable` آمن) و`/api/site-content` (محتوى عام، 60 ثانية فقط)، إضافة
  إلى ملفات `_next/static` المُجزّأة بالأسماء (تتغير أسماؤها مع كل بناء).
- **تصحيح (2026-08-27)**: مشرف النظام الذي جهته في UAE PASS «وزارة شؤون
  مجلس الوزراء» كان قد يُحتجز في نسخة الوزارة `/moca` بلا وصول للوحة
  المشرف. أصبح `/moca` يعيد توجيه أصحاب دور `system_admin` تلقائياً إلى
  `/dashboard` حيث لوحاتهم الموحدة وزر «لوحة الإدارة» ومبدّل الأدوار —
  تم التحقق مباشرة على خادم بقاعدة بيانات حقيقية وجلسة SSO ممثلة.
