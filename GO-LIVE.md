# AIGP — حزمة الإطلاق النهائية (Go-Live)

**الفرع:** `it-after-changes-2` · **تاريخ الإصدار:** 2026-08-18

هذه هي الحزمة النهائية للنشر على البيئة الحية. بنية قاعدة البيانات **هي
نفسها المعتمدة سابقاً دون أي تغيير** — لا ترحيلات جديدة منذ حزمة staging.
خطوات النشر التفصيلية في `DEPLOYMENT.md`، والأدوار والصلاحيات في
`docs/roles-pages-access.md`، وسجل التغييرات الكامل في
`docs/CHANGES-2026-08-11.md` بملاحقه.

---

## 1. قاعدة البيانات — البنية نفسها المعتمدة سابقاً

مجموعة الترحيلات: **`0001 … 0011`** كما هي، يليها ترحيلا التحصين اللذان
أضافهما فريق IT في نسخة v4 بعد **إعادة ترقيمهما**:

- `0006_rbac_security` ← صار **`0012_rbac_hardening_v3`**
- `0007_role_assignment_rules` ← صار **`0013_role_assignment_rules`**

> **سبب إعادة الترقيم:** بترقيمهما القديم كانا يسبقان
> `0007_roles_sessions_notifications` فيفشل الترتيب على قاعدة بيانات
> جديدة (`relation "roles" already exists`). محتواهما لم يتغيّر (جُعلت
> قيود FK فيهما idempotent فقط) — وعلى قاعدة staging القائمة التي طُبّقا
> عليها بالاسمين القديمين سيعيد `migrate deploy` تشغيلهما بالاسمين
> الجديدين بأمان (كلاهما idempotent) مع تحذيرٍ عن الاسمين القديمين
> المسجّلين في `_prisma_migrations` يمكن تجاهله.

```bash
npx prisma migrate deploy              # 0001 → 0013
npm run db:seed                        # مرجعية — idempotent
npx tsx prisma/seed-deputy-role.ts     # دور stream_deputy (اختياري — يُدمج في فريق العمل بالواجهة)
```

الحقول التي أُضيفت بعد `0011` كلها داخل العمود القائم `items.activities`
(jsonb) ولا تحتاج ترحيلاً:

| داخل كل نشاط في `activities` | القيم | الغرض |
|---|---|---|
| `batchWf` | `draft` \| `pending` \| `approved` | حالة اعتماد التوزيع على الدفعة |
| `batchRet` | `info` \| `reject` | نوع قرار الإعادة/الرفض على التوزيع |
| `batchNote` | نص | سبب الإعادة/الرفض — يظهر للمنسق |

قواعد يطبّقها الخادم (تفصيلها في ملحق 2026-08-14 من وثيقة التغييرات):

1. دورتا اعتماد منفصلتان: اعتماد محتوى المدخل (كما هو) واعتماد التوزيع
   على الدفعات (جديدة). لا تمسّ إحداهما حالة الأخرى.
2. لا يُوزَّع على دفعات الإطلاق إلا مدخل معتمد (`exec/launch/done`) —
   غير ذلك يُرفض 409.
3. التوزيع `pending` أو `approved` مقفل: نقل أو إزالة أو تعديل تواريخ → 409.
4. `POST /api/items/batch-drafts/:streamId/submit` يرسل التوزيعات
   بجسم `{ keys?: ["<itemId>::<actIdx>"] }` — كلها عند الإغفال.
5. قرار التوزيع: `approve` يقفل؛ `info`/`reject` يعيدانه `draft` مع
   `batchRet`/`batchNote`.

### دمج نسخة IT (aigp-v4-portal)

هذه الحزمة مبنية على نسخة `aigp-v4-portal` التي عدّلها فريق IT، مضافاً
إليها كل تغييرات المنصة اللاحقة. ما بقي من نسخة IT كما هو: التحصين
الأمني (V3)، لوحة الإدارة المتصلة بـ `/api/admin/*`، تشغيل الترحيلات
عند الإقلاع (Docker/K8s)، بريد المنسقين المعتمد، ومسار الدخول
`/api/auth/login`. التعديل الوحيد على عملهم: إعادة ترقيم الترحيلين
أعلاه، وربط دور `stream_deputy` الخلفي بدور «فريق عمل المسار في
المشروع» في الواجهة وفق البنية المعتمدة (أربعة أدوار).

## 2. الأدوار — البنية المعتمدة (أربعة أدوار)

| دور الواجهة | الاسم | رمز RBAC | النطاق |
|---|---|---|---|
| `coord` | منسق المسار في الجهة الاتحادية | `entity_coordinator` | جهته + مساره (يشمل المسودات) |
| `path` | فريق عمل المسار في المشروع | `stream_owner` | مساره عبر كل الجهات — معتمِد الدورتين |
| `ai` | اللجنة الوطنية للذكاء الاصطناعي المساعد | `ai_committee` | وطني — المعتمد فقط (اطلاع) |
| `admin` | مشرف النظام | `system_admin` | النظام بالكامل |

- الدوران الملغيان (`deputy`، `secretariat`) لم يكن لهما وجود في جدول
  `roles` — الدمج لا يغيّر أي صلاحية. الحسابات المحفوظة بهما تُرحَّل
  تلقائياً في الواجهة، وللتنظيف المباشر:

```sql
UPDATE users SET role = 'path' WHERE role = 'deputy';
UPDATE users SET role = 'ai'   WHERE role = 'secretariat';
```

- إعادة تشغيل البذر تحدّث مسميات `roles.name_ar` للأدوار الثلاثة
  المعاد تسميتها (idempotent).
- الأدوار الخلفية الأخرى (`entity_representative`، `entity_admin`،
  `viewer`، `auditor`، `program_admin`) تبقى مبذورة للتوافق ولا تملك
  صلاحيات اعتماد.

## 3. نسخة وزارة شؤون مجلس الوزراء

مسار مستقل `/moca` (حصر المهام والعمليات — 19 حقلاً مطابقة للنموذج
المعتمد، 9 جهات/مكاتب بقطاعاتها، الاعتماد مباشرة للجنة الوطنية).
وحدة معزولة تماماً عن منصة الجهات — تخزينها حالياً في المتصفح؛ إن
أريد ربطها بقاعدة البيانات فذلك ترحيل مستقل (`0012`) يُتفق عليه لاحقاً
ولا يمسّ البنية الحالية.

## 4. نتائج التحقق النهائي قبل التسليم (على PostgreSQL 16 نظيف)

| الفحص | النتيجة |
|---|---|
| `prisma migrate deploy` على قاعدة فارغة | ✅ 11 ترحيلاً طُبّقت |
| `npm run db:seed` | ✅ نجح |
| جدول `roles` بعد البذر | ✅ المسميات الأربعة المعتمدة |
| `service_catalog` | ✅ 1855 صفاً · 47 جهة |
| `items.activities` | ✅ jsonb |
| `npm run build` (نسخة الخادم الكاملة بكل مسارات API) | ✅ صفر أخطاء |

## 5. أبرز ما يتضمنه هذا الإصدار منذ حزمة staging السابقة

- دورة اعتماد التوزيع على الدفعات كاملةً (إرسال الكل أو توزيعاً واحداً،
  قرار فريق العمل بسبب إلزامي، قفل المعتمد) وعموداً حالة منفصلان.
- قصر التوزيع على المدخلات المعتمدة.
- إشعارات مصحّحة لكل دور (توزيعات معلّقة لفريق العمل تزول بالبتّ،
  وقرارات التوزيع بأسبابها للمنسق).
- صفحة وزارة شؤون مجلس الوزراء `/moca` بتصميم المنصة نفسه.
- تقليص الأدوار إلى البنية المعتمدة، الرفض حالة مستقلة «تم الرفض»،
  الإزالة لكل غير معتمد، دليل الخدمات مقيّد بالجهة، وكل تحسينات الواجهة
  الموثقة في سجل التغييرات.
