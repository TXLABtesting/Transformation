# AIGP — حزمة الإطلاق النهائية (Go-Live)

**الفرع:** `it-after-changes-2` · **تاريخ الإصدار:** 2026-08-22

هذه هي الحزمة النهائية للنشر على البيئة الحية. بنية قاعدة البيانات **هي
نفسها المعتمدة سابقاً دون أي تغيير** — لا ترحيلات جديدة منذ حزمة staging.
خطوات النشر التفصيلية في `DEPLOYMENT.md`، والأدوار والصلاحيات في
`docs/roles-pages-access.md`، وسجل التغييرات الكامل في
`docs/CHANGES-2026-08-11.md` بملاحقه.

---

## 1. قاعدة البيانات — البنية نفسها المعتمدة سابقاً

مجموعة الترحيلات: **`0001 … 0011`** كما هي، يليها ترحيلا التحصين اللذان
أضافهما فريق IT في نسخة v4 بعد **إعادة ترقيمهما**:

- `0006_rbac_security` ← صار **`0012_rbac_hardening_v3`**
- `0007_role_assignment_rules` ← صار **`0013_role_assignment_rules`**

> **سبب إعادة الترقيم:** بترقيمهما القديم كانا يسبقان
> `0007_roles_sessions_notifications` فيفشل الترتيب على قاعدة بيانات
> جديدة (`relation "roles" already exists`). محتواهما لم يتغيّر (جُعلت
> قيود FK فيهما idempotent فقط) — وعلى قاعدة staging القائمة التي طُبّقا
> عليها بالاسمين القديمين سيعيد `migrate deploy` تشغيلهما بالاسمين
> الجديدين بأمان (كلاهما idempotent) مع تحذيرٍ عن الاسمين القديمين
> المسجّلين في `_prisma_migrations` يمكن تجاهله.

```bash
npx prisma migrate deploy              # 0001 → 0013
npm run db:seed                        # مرجعية — idempotent
```

> **لا يوجد دور نائب.** أُزيل ما أضافته نسخة v4 من دور `stream_deputy`
> (السكربت والربط في الكود). إن كان الدور قد أُنشئ على قاعدة staging،
> يُنظَّف هكذا — تُنقل حساباته إن وُجدت إلى `stream_owner` ثم يُحذف:
>
> ```sql
> UPDATE user_roles SET role_id = (SELECT id FROM roles WHERE code='stream_owner')
>  WHERE role_id = (SELECT id FROM roles WHERE code='stream_deputy')
>    AND NOT EXISTS (SELECT 1 FROM user_roles ur2
>                     WHERE ur2.user_id = user_roles.user_id
>                       AND ur2.role_id = (SELECT id FROM roles WHERE code='stream_owner'));
> DELETE FROM user_roles       WHERE role_id = (SELECT id FROM roles WHERE code='stream_deputy');
> DELETE FROM role_permissions WHERE role_id = (SELECT id FROM roles WHERE code='stream_deputy');
> DELETE FROM roles            WHERE code = 'stream_deputy';
> ```

الحقول التي أُضيفت بعد `0011` كلها داخل العمود القائم `items.activities`
(jsonb) ولا تحتاج ترحيلاً:

| داخل كل نشاط في `activities` | القيم | الغرض |
|---|---|---|
| `batchWf` | `draft` \| `pending` \| `approved` | حالة اعتماد التوزيع على الدفعة |
| `batchRet` | `info` \| `reject` | نوع قرار الإعادة/الرفض على التوزيع |
| `batchNote` | نص | سبب الإعادة/الرفض — يظهر للمنسق |

قواعد يطبّقها الخادم (تفصيلها في ملحق 2026-08-14 من وثيقة التغييرات):

1. دورتا اعتماد منفصلتان: اعتماد محتوى المدخل (كما هو) واعتماد التوزيع
   على الدفعات (جديدة). لا تمسّ إحداهما حالة الأخرى.
2. لا يُوزَّع على دفعات الإطلاق إلا مدخل معتمد (`exec/launch/done`) —
   غير ذلك يُرفض 409.
3. التوزيع `pending` أو `approved` مقفل: نقل أو إزالة أو تعديل تواريخ → 409.
4. `POST /api/items/batch-drafts/:streamId/submit` يرسل التوزيعات
   بجسم `{ keys?: ["<itemId>::<actIdx>"] }` — كلها عند الإغفال.
5. قرار التوزيع: `approve` يقفل؛ `info`/`reject` يعيدانه `draft` مع
   `batchRet`/`batchNote`.

### دمج نسخة IT (aigp-v4-portal)

هذه الحزمة مبنية على نسخة `aigp-v4-portal` التي عدّلها فريق IT، مضافاً
إليها كل تغييرات المنصة اللاحقة. ما بقي من نسخة IT كما هو: التحصين
الأمني (V3)، لوحة الإدارة المتصلة بـ `/api/admin/*`، تشغيل الترحيلات
عند الإقلاع (Docker/K8s)، بريد المنسقين المعتمد، ومسار الدخول
`/api/auth/login`. التعديل الوحيد على عملهم: إعادة ترقيم الترحيلين
أعلاه، وإزالة دور `stream_deputy` الذي كانت النسخة قد أضافته — لا وجود
لدور نائب في البنية المعتمدة (أربعة أدوار).

## 2. الأدوار — البنية المعتمدة (أربعة أدوار)

| دور الواجهة | الاسم | رمز RBAC | النطاق |
|---|---|---|---|
| `coord` | منسق المسار في الجهة الاتحادية | `entity_coordinator` | جهته + مساره (يشمل المسودات) |
| `path` | فريق عمل المسار في المشروع | `stream_owner` | مساره عبر كل الجهات — معتمِد الدورتين |
| `ai` | اللجنة الوطنية للذكاء الاصطناعي المساعد | `ai_committee` | وطني — المعتمد فقط (اطلاع) |
| `admin` | مشرف النظام | `system_admin` | النظام بالكامل |

- الدوران الملغيان (`deputy`، `secretariat`) لم يكن لهما وجود في جدول
  `roles` — الدمج لا يغيّر أي صلاحية. الحسابات المحفوظة بهما تُرحَّل
  تلقائياً في الواجهة، وللتنظيف المباشر:

```sql
UPDATE users SET role = 'path' WHERE role = 'deputy';
UPDATE users SET role = 'ai'   WHERE role = 'secretariat';
```

- إعادة تشغيل البذر تحدّث مسميات `roles.name_ar` للأدوار الثلاثة
  المعاد تسميتها (idempotent).
- الأدوار الخلفية الأخرى (`entity_representative`، `entity_admin`،
  `viewer`، `auditor`، `program_admin`) تبقى مبذورة للتوافق ولا تملك
  صلاحيات اعتماد.

## 3. نسخة وزارة شؤون مجلس الوزراء

مسار مستقل `/moca` (حصر المهام والعمليات — 19 حقلاً مطابقة للنموذج
المعتمد، 9 جهات/مكاتب بقطاعاتها، الاعتماد مباشرة للجنة الوطنية).
وحدة معزولة تماماً عن منصة الجهات — تخزينها حالياً في المتصفح؛ إن
أريد ربطها بقاعدة البيانات فذلك ترحيل مستقل (`0012`) يُتفق عليه لاحقاً
ولا يمسّ البنية الحالية.

## 4. نتائج التحقق النهائي قبل التسليم (على PostgreSQL 16 نظيف)

| الفحص | النتيجة |
|---|---|
| `prisma migrate deploy` على قاعدة فارغة | ✅ 11 ترحيلاً طُبّقت |
| `npm run db:seed` | ✅ نجح |
| جدول `roles` بعد البذر | ✅ المسميات الأربعة المعتمدة |
| `service_catalog` | ✅ 1855 صفاً · 47 جهة |
| `items.activities` | ✅ jsonb |
| `npm run build` (نسخة الخادم الكاملة بكل مسارات API) | ✅ صفر أخطاء |

## 5. أبرز ما يتضمنه هذا الإصدار منذ حزمة staging السابقة

- دورة اعتماد التوزيع على الدفعات كاملةً (إرسال الكل أو توزيعاً واحداً،
  قرار فريق العمل بسبب إلزامي، قفل المعتمد) وعموداً حالة منفصلان.
- قصر التوزيع على المدخلات المعتمدة.
- إشعارات مصحّحة لكل دور (توزيعات معلّقة لفريق العمل تزول بالبتّ،
  وقرارات التوزيع بأسبابها للمنسق).
- صفحة وزارة شؤون مجلس الوزراء `/moca` بتصميم المنصة نفسه.
- تقليص الأدوار إلى البنية المعتمدة، الرفض حالة مستقلة «تم الرفض»،
  الإزالة لكل غير معتمد، دليل الخدمات مقيّد بالجهة، وكل تحسينات الواجهة
  الموثقة في سجل التغييرات.

## 6. تحديث ما بعد الحزمة: الموقع العام الجديد وبنية المسارات (بلا أي تغيير على قاعدة البيانات)

**المسارات الجديدة** (كلها صفحات عميل ثابتة تُبنى مع التطبيق نفسه):

| المسار | الصفحة |
|---|---|
| `/` | الصفحة الرئيسية العامة (فيديو الواجهة، المستهدفات، الأخبار، المسارات، البرنامج الزمني) |
| `/about` | من نحن (مسيرة التحول، اقتباس القيادة، المبادئ) |
| `/library` | المنشورات (تُغذّى من «وثائق المنشورات» في لوحة المشرف) |
| `/contact` | تواصل معنا (يصب في صندوق استفسارات المسار كما كان) |
| `/login` | صفحة تسجيل الدخول — زر UAE PASS يستدعي `/api/auth/login` كما هو |
| `/dashboard` | لوحات المنصة كما كانت على `/` سابقاً — بلا أي تغيير وظيفي |
| `/moca` | نسخة الوزارة كما هي |

**تدفق الدخول**: الزائر يرى الرئيسية ومن نحن فقط؛ بعد OIDC يعود المستخدم
إلى `/` (إعادة توجيه callback الحالية نفسها) ويظهر شريط الأعضاء وزر
«منصة الإدخال» الذي يوجّه مستخدم وزارة شؤون مجلس الوزراء إلى `/moca`
وبقية الجهات إلى `/dashboard`.

**تغييران فقط على طبقة الخادم — ولا ترحيلات جديدة:**

1. **`GET /api/site-content` (جديد، قراءة عامة بلا جلسة)**: يعيد الجزء
   العام فقط من كتلة `app_state` (محتوى الموقع الذي يحرره المشرف +
   وثائق المنشورات + النص التعريفي). لا يتسرب منه أي من بيانات المسارات
   أو الجهات أو المستخدمين، والكتابة تبقى حصراً عبر `PUT /api/state`
   بصلاحياتها الحالية (جلسة + نطاق شامل + رمز `STATE_API_TOKEN` إن ضُبط).
2. **رفع سقف `PUT /api/state` من 2MB إلى 10MB**: كتلة الحالة أصبحت تحمل
   محتوى الموقع العام بصوره المضغوطة (أخبار/محطات/أغلفة كـ data URLs).

**لوحة المشرف**: تبويب «الموقع العام» أصبح CMS كاملاً للصفحات الجديدة
(العناوين، فيديو الواجهة بالرابط، المستهدفات، الأخبار، المسارات، المراحل،
محطات مسيرة التحول بصورها، الاقتباس، المبادئ، نص تواصل معنا).
ملاحظة تشغيلية: الفيديوهات والملفات الكبيرة يُفضّل استضافتها كروابط
(الحقل المخصص في اللوحة) لا كملفات مضمّنة.

**الأصول**: أُضيفت أصول الموقع العام تحت `public/assets/web` (~13MB —
صور وفيديو الواجهة). لا تغيير على `package.json` ولا على Docker ولا على
متغيرات البيئة ولا على الترحيلات (تبقى 0001–0013 كما وثّق أعلاه).

## 7. استفسارات «تواصل معنا» — لوحة المشرف فقط والبريد لكل مسار

- **`POST /api/contact` (عام بلا جلسة)**: يسجّل استفسار الزائر في كتلة
  الحالة (`app_state` — بلا ترحيل جديد) ثم يرسله بالبريد إلى عنوان
  المسار المعيّن في لوحة المشرف.
- **الاطلاع للمشرف فقط**: قائمة الاستفسارات تظهر حصراً في لوحة المشرف
  («التواصل والاستفسارات» — ربط بريد كل مسار + الاستفسارات الواردة
  بحالة المعالجة). فرق المسارات واللجنة الوطنية **تستقبل الاستفسارات
  بالبريد فقط** ولا صندوق لها في اللوحات.
- **`GET /api/contact`** و**`PATCH`** (تبديل «تمت المعالجة») مقيّدان
  بجلسة مصادَق عليها بنطاق شامل (`system_admin`) — 401 بلا جلسة و403
  لغير النطاق الشامل؛ و`PUT /api/state` يدمج الاستفسارات بالاتحاد حتى
  لا تُمسح الاستفسارات الواصلة بين مزامنتين.
- **البريد اختياري التفعيل**: يُرسل فقط عند ضبط متغيرات البيئة —
  `SMTP_HOST` (إلزامي للتفعيل) و`SMTP_PORT` (افتراضي 587) و`SMTP_USER`/
  `SMTP_PASS` و`SMTP_FROM` و`SMTP_SECURE=1` للاتصال المشفّر المباشر.
  بلا تهيئة SMTP يبقى كل شيء يعمل (التسجيل والصناديق) وتُتجاهل خطوة
  البريد. ربط عنوان كل مسار من لوحة المشرف ← «التواصل والاستفسارات».
- **اعتمادية جديدة واحدة**: `nodemailer` (في `package.json`) — تُثبّت مع
  `npm ci` كالمعتاد ولا تتطلب أي إعداد إضافي.

## 8. مخزن وسائط الموقع العام — ترحيل جديد واحد (`0014_site_media`)

لتمكين المشرف من رفع صور وفيديو الصفحات العامة **كمرفقات** بجودتها الكاملة
(بدل روابط خارجية أو صور مضغوطة داخل كتلة الحالة):

- **ترحيل جديد**: `0014_site_media` — جدول مستقل `site_media` لا يمسّ أي
  جدول قائم؛ يُطبَّق بـ `prisma migrate deploy` كالمعتاد (0001 → 0014).
- **`POST /api/media`** (مشرف النظام فقط): يستقبل الملف كما هو — صور حتى
  15MB وفيديو mp4/webm حتى 200MB — ويعيد رابط `/api/media/:id`.
- **`GET /api/media/:id`** (عام): يخدم الملف بنوعه الصحيح مع
  `Cache-Control: public, max-age=31536000, immutable` و`ETag` ودعم
  `Range` لتشغيل الفيديو والتنقل فيه. الاستبدال يُنشئ معرّفاً جديداً،
  فالتخزين المؤقت الدائم آمن ولا يتحمّل الخادم إلا أول طلب من كل متصفح.
- **`DELETE /api/media/:id`** (مشرف النظام): تستدعيه اللوحة تلقائياً عند
  استبدال ملف أو الاستغناء عنه — فلا تتراكم ملفات مهجورة.
- كتلة الحالة تخزّن الروابط فقط (نصوص قصيرة) — لا أثر للوسائط على حجمها
  ولا على سرعة `/api/site-content`.

## 9. معالجة نتائج فحص TDRA/Checkmarx (تقرير CA_26_76 — 2026-08-26)

نتيجتان فقط في التقرير (لا حرجة ولا عالية)، وكلتاهما عولجتا في الكود:

- **Missing HSTS Header (متوسطة)**: كانت الترويسة مضبوطة أصلاً عبر
  `headers()` في `next.config.mjs` على كل المسارات، لكن الفاحص الساكن لا
  يتعرّف على هذا الشكل. أُضيف `middleware.ts` يضبط
  `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`
  برمجياً على **جميع** الاستجابات (صفحات، API، ملفات ثابتة) — تم التحقق
  بالفحص المباشر على الخادم. لا تغيير على Docker أو خطوات النشر؛ الملف
  يُلتقط تلقائياً في `next build`.
  **ملاحظة للنشر**: إن كان أمام التطبيق مُوزِّع/Proxy يُنهي TLS فتأكدوا
  ألا يحذف الترويسة، وأن يكون الوصول عبر HTTPS دائماً.
- **Unsafe Use Of Target blank (منخفضة)** في
  `components/site/PublicLibrary.tsx`: أصبح فتح النوافذ بالنمط الموصى به
  (`win.opener = null` قبل أي استخدام)، وأضيف `noopener` لكل روابط
  `target="_blank"` المتبقية.
- **Cache-Control (ملاحظة متابعة)**: أصبح `middleware.ts` يرسل
  `Cache-Control: no-store, no-cache, must-revalidate` و`Pragma: no-cache`
  على **كل** استجابات API الديناميكية (الجلسات وبيانات المستخدمين واللوحات)،
  فلا يخزّنها متصفح ولا وسيط. التخزين العام يبقى محصوراً بموردين غير
  حساسين مُهيّأين بإصدارات: `/api/media/:id` (معرّف جديد عند كل استبدال —
  `immutable` آمن) و`/api/site-content` (محتوى عام، 60 ثانية فقط)، إضافة
  إلى ملفات `_next/static` المُجزّأة بالأسماء (تتغير أسماؤها مع كل بناء).
- **تصحيح (2026-08-27)**: مشرف النظام الذي جهته في UAE PASS «وزارة شؤون
  مجلس الوزراء» كان قد يُحتجز في نسخة الوزارة `/moca` بلا وصول للوحة
  المشرف. أصبح `/moca` يعيد توجيه أصحاب دور `system_admin` تلقائياً إلى
  `/dashboard` حيث لوحاتهم الموحدة وزر «لوحة الإدارة» ومبدّل الأدوار —
  تم التحقق مباشرة على خادم بقاعدة بيانات حقيقية وجلسة SSO ممثلة.

## 10. المشاريع الاستراتيجية ونسخة الوزارة على قاعدة البيانات — ترحيل `0015`

### ما الجديد
- **المشاريع الاستراتيجية**: اللجنة الوطنية تعرّف المشاريع وتسندها إلى القادة
  وأعضائهم، والعضو يعبّئ نموذج المشروع (بيانات المشروع، المراحل التنفيذية،
  فريق العمل) ويرسله، واللجنة تعتمده أو تعيده بملاحظات.
- **نسخة الوزارة**: مدخلات جهات وزارة شؤون مجلس الوزراء وحالات استخدامها
  وتوزيعاتها على دفعات الإطلاق انتقلت من تخزين المتصفح إلى قاعدة البيانات —
  فما يرسله منسق الوزارة يصل اللجنة فعلاً عبر الخادم، وتظهر الوزارة كجهة ضمن
  قوائم اللجنة (قوائم الحصر ودفعات الإطلاق وحالات الاستخدام) مع الاعتماد.

### قاعدة البيانات — إضافة فقط، بلا أي تغيير في البنية القائمة
- **ترحيل واحد جديد**: `0015_moca_and_strategic_projects` — خمسة جداول
  مستقلة: `moca_entries`، `moca_use_cases`، `proj_defs`، `proj_forms`،
  `proj_member_leads`. **لا `ALTER` ولا `DROP` على أي جدول قائم**، ولا تغيير
  في خطوات النشر: `prisma migrate deploy` كالمعتاد (0001 → 0015).
- **بذرة إضافية فقط**: دور `strategic_project_member` باسمه العربي
  «أعضاء المشاريع الاستراتيجية» وصلاحياته (اطلاع فقط على المرجعيات، بلا أي
  صلاحية على مدخلات المسارات). لا تعديل على الأدوار القائمة.
- **إسناد العضو إلى قائده** في جدول ربط مستقل (`proj_member_leads`) بدل
  إضافة عمود على جدول المستخدمين.

### الواجهات البرمجية الجديدة (بحُرّاس الجلسة والصلاحيات القائمة نفسها)
| المسار | الطريقة | من |
|---|---|---|
| `/api/moca/entries` | GET | منسوبو الوزارة (نطاقهم) واللجنة/المشرف (المُرسَل وما بعده) |
| `/api/moca/use-cases` | GET | كما سبق |
| `/api/moca/sync` | POST | المنسق: محتواه فقط · اللجنة/المشرف: حقول القرار فقط |
| `/api/projects/defs` | GET/POST | القراءة للجنة والأعضاء · الإنشاء للجنة/المشرف |
| `/api/projects/defs/:id` | PATCH/DELETE | اللجنة/المشرف |
| `/api/projects/forms` | GET/POST | القراءة للجنة والأعضاء · الكتابة للعضو |
| `/api/projects/forms/:id` | PATCH/DELETE | الاعتماد/الإعادة للجنة · الحذف للعضو |
| `/api/projects/member-leads` | GET/PUT | القراءة للمعنيين · الإسناد بصلاحية `users:update` |

### إسناد وحدة لمنسق الوزارة (اختياري)
منسق الوزارة يعمل افتراضياً على مستوى الوزارة كاملة. لقصره على وحدة أو قطاع
يُضاف سطر في جدول الإعدادات القائم `settings` — بلا أي تغيير في البنية:
```sql
INSERT INTO settings (key, value) VALUES ('moca_unit:<USER_ID>', 'sg');
-- أو مع قطاع:  ('moca_unit:<USER_ID>', 'pmo::قطاع الخدمات الحكومية')
```

### تصحيح أمني مرافق
كتلة الحالة المشتركة (`app_state`) كانت تحمل هوية المستخدم (الدور والجهة
والمسار) لا محتواه فقط، فكان صاحب دور عام يرث دور آخر شارَكه الكتلة (ظهرت
لوحة المشرف لعضو اللجنة). أصبحت الهوية تُشتق من `/api/auth/me` حصراً.

### التحقق قبل التسليم
على قاعدة بيانات PostgreSQL نظيفة (15 ترحيلاً + البذرة + بناء SSO):
**25 فحصاً على الواجهات البرمجية** و**20 فحصاً في المتصفح** — كلها ناجحة،
وتشمل: وصول مدخل منسق الوزارة إلى اللجنة عبر جلستين منفصلتين، اعتماد اللجنة
ورجوعه للمنسق، رفض اعتماد المنسق لنفسه، حجب الأدوار الأخرى عن بيانات الوزارة،
دورة المشاريع الاستراتيجية كاملة (تعريف → تعبئة → إعادة بملاحظات → اعتماد)،
منع العضو من اعتماد نموذجه أو تعريف مشاريع، وسلامة الواجهات القائمة
(`/api/items`، `/api/admin/users`، `/api/launch-plans`، `/api/auth/me`)
وترويسات الأمان.

---

# ١١) تسليم ٢٧ أغسطس ٢٠٢٦ — الأدوار من قاعدة البيانات والنطاقات المتعددة

## ١١-١ ما الذي تغيّر؟

| # | التغيير | الأثر على قاعدة البيانات |
|---|---------|---------------------------|
| ١ | إصلاح خطأ إضافة مستخدم (كان يعيد 500 مبهماً) | لا شيء |
| ٢ | تصحيح دور مشرف النظام في العمود القديم | صف واحد لكل مشرف قائم (اختياري) |
| ٣ | قائمة الأدوار من قاعدة البيانات لا من الشيفرة | لا شيء |
| ٤ | نطاق كل دور (حقول النموذج) | صفوف `settings` اختيارية |
| ٥ | مسارات متعددة للمستخدم | يستخدم `user_stream_scopes` القائم |
| ٦ | وحدات وقطاعات متعددة داخل الوزارة | صفوف `settings` بمفتاح `moca_unit:` |
| ٧ | حذف نهائي للحسابات من لوحة المشرف | لا شيء |
| ٨ | الحسابات التجريبية لم تعد تُزرع | يتطلب إنشاء المستخدمين الحقيقيين |
| ٩ | استجابة لوحة المشرف على الجوال | لا شيء |

**لا ترحيل جديد (migration) في هذا التسليم.** آخر ترحيل هو `0015` المسلَّم سابقاً.

## ١١-٢ المطلوب من فريق تقنية المعلومات

### أ) إنشاء المستخدمين (مطلوب)

قاعدة البيانات لم تعد تُنشئ حسابات تجريبية. بعد `prisma migrate deploy` و`npm run db:seed`
تحتوي القاعدة على البيانات المرجعية فقط: الأدوار، الصلاحيات، المسارات، الجهات، مراحل البرنامج.

يُنشأ المستخدمون بإحدى طريقتين:

1. **من لوحة المشرف** (المفضّل): يُنشأ أول حساب مشرف مباشرة في القاعدة، ثم تُدار البقية من الواجهة.
2. **مباشرة في القاعدة** لأول مشرف:

```sql
-- ١) الحساب: البريد نفسه المسجّل في UAE PASS
INSERT INTO users (id, email, name, title, role, status, access_enabled, is_active, created_at, updated_at)
VALUES (gen_random_uuid()::text, 'ayoub.bari@moca.gov.ae', 'أيوب باري', 'مشرف النظام',
        'admin', 'active', true, true, NOW(), NOW());

-- ٢) دور الصلاحيات: مشرف النظام
INSERT INTO user_roles (id, user_id, role_id, created_at)
SELECT gen_random_uuid()::text, u.id, r.id, NOW()
FROM users u, roles r
WHERE u.email = 'ayoub.bari@moca.gov.ae' AND r.code = 'system_admin';
```

> **مهم:** عمود `users.role` (القديم) للواجهة فقط. الصلاحيات الفعلية من `user_roles`.
> يجب أن يكون `access_enabled = true` وإلا رُفض الدخول برسالة «لم يتم تفعيل حسابك».

### ب) تصحيح المشرفين القائمين (إن وُجدوا على staging)

كان دور `system_admin` يُكتب في العمود القديم بقيمة `ai`، فيظهر المشرف في القوائم
على أنه عضو اللجنة الوطنية. الواجهة صارت تقرأ دور الصلاحيات فتظهر صحيحة،
ولتنظيف البيانات نفسها:

```sql
UPDATE users u SET role = 'admin'
FROM user_roles ur JOIN roles r ON r.id = ur.role_id
WHERE ur.user_id = u.id AND r.code = 'system_admin' AND u.role <> 'admin';
```

### ج) إسناد وحدات وزارة شؤون مجلس الوزراء (اختياري)

منسق الوزارة بلا إسناد يعمل على مدخلات الوزارة كاملة. لقصره على وحدة أو أكثر:

```sql
-- وحدة واحدة
INSERT INTO settings (key, value) VALUES ('moca_unit:<USER_ID>', 'sg')
ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;

-- عدة وحدات: مفصولة بـ | والقطاع بعد ::
INSERT INTO settings (key, value)
VALUES ('moca_unit:<USER_ID>', 'sg|pmo::قطاع الخدمات الحكومية')
ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
```

معرّفات الوحدات: `sg` (الأمانة العامة لمجلس الوزراء)، `pmo` (مكتب رئاسة مجلس الوزراء وقطاعاته).
ويمكن إسنادها كذلك من لوحة المشرف مباشرة دون SQL.

### د) ضبط نطاق دور (اختياري)

نطاق الدور يحدّد الحقول التي يطلبها نموذج إضافة المستخدم. يُشتق تلقائياً من صلاحيات
الدور، ويمكن تثبيته صراحةً:

```sql
INSERT INTO settings (key, value) VALUES ('role_scope:entity_coordinator', 'entity_stream')
ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
```

القيم المقبولة: `global` | `stream` | `entity` | `entity_stream` | `none`.

| النطاق | الجهة | المسارات | الأدوار عليه حالياً |
|--------|-------|----------|---------------------|
| `global` | — | — | system_admin، program_admin، ai_committee |
| `stream` | اختيارية | مطلوبة | stream_owner |
| `entity_stream` | مطلوبة | مطلوبة | entity_coordinator، entity_admin |
| `entity` | مطلوبة | — | entity_representative، viewer، auditor |
| `none` | — | — | strategic_project_member |

### هـ) متغيّرات البيئة (بلا تغيير عن التسليم السابق)

```
DATABASE_URL=postgresql://…
SESSION_SECRET=<سلسلة عشوائية طويلة>
NEXT_PUBLIC_DATA_MODE=api
NEXT_PUBLIC_AUTH_PROVIDER=uaepass
```

`SEED_DEMO_USERS=1` لقاعدة عرض أو اختبار فقط — لا تُستخدم على staging أو الإنتاج.

## ١١-٣ إضافة دور جديد لاحقاً

لا تتطلب أي تعديل في الشيفرة:

```sql
INSERT INTO roles (id, code, name_ar, is_system, created_at)
VALUES (gen_random_uuid()::text, 'new_role_code', 'المسمى بالعربية', false, NOW());

INSERT INTO role_permissions (role_id, permission_id)
SELECT r.id, p.id FROM roles r, permissions p
WHERE r.code = 'new_role_code' AND p.code IN ('items:view','reports:view');

-- اختياري: تثبيت نطاقه
INSERT INTO settings (key, value) VALUES ('role_scope:new_role_code', 'entity');
```

يظهر الدور فوراً في نموذج إضافة المستخدم بحقوله الصحيحة.

## ١١-٤ نتائج الفحص على قاعدة بيانات نظيفة

٢١ فحصاً على طبقة الخادم و٢٤ فحصاً للاستجابة — كلها ناجحة:

- رسائل أخطاء إضافة المستخدم (بريد مكرر ٤٠٩، مرجع غير موجود ٤٠٠)
- الأدوار العشرة تصل من القاعدة بنطاقاتها الصحيحة
- إسناد مسارين لمستخدم وقراءتهما في جلسته، ورفض مسار غير موجود
- إسناد وحدتين داخل الوزارة، والكتابة فيهما، **ورفض الكتابة في وحدة ثالثة خارج نطاقه**
- الاستجابة على ٣٩٠ و٤٣٠ و٧٦٨ و١٠٢٤ و١٢٨٠ و١٦٠٠ بكسل بلا تمرير أفقي

---

# ١٢) تسليم ٣١ أغسطس ٢٠٢٦ — العضو بالبريد وقائد المشاريع الاستراتيجية

## ١٢-١ ما الجديد

- **إسناد العضو من نموذج المشروع مباشرة**: اللجنة تختار العضو المسؤول من
  الأعضاء المسجلين، أو تُدخل بريده الإلكتروني فيُنشأ له حساب فوراً بدور
  «أعضاء المشاريع الاستراتيجية» مفعّلاً وتحت قائد المشروع — يدخل عبر UAE PASS
  بالبريد نفسه ويجد مشروعه بانتظار التعبئة.
- **كل عضو يرى مشاريعه المسندة إليه فقط** (كانت كل المشاريع تظهر لكل الأعضاء).
- **دور جديد «قائد المشاريع الاستراتيجية» (strategic_project_lead)**: اطلاع
  فقط على مشاريع قيادته وحالة تعبئة نماذجها — لا يكتب ولا يعتمد ولا يعرّف.
  يُسند من لوحة المشرف مع تحديد «هوية القائد» (أي قائد من القادة المعتمدين).
- إصلاح: الحساب الجديد بلا جهة لم يعد يرث جهة المشرف الذي أنشأه، ومسح
  الجهة/المسار من محرر المستخدم صار يُحفظ فعلاً.

## ١٢-٢ المطلوب من IT

1. `prisma migrate deploy` — يطبّق **الترحيل 0016**: عمودا `member_id` و
   `member_email` وفهرس على جدول `proj_defs` (المُنشأ في 0015). إضافي بالكامل،
   لا يمس أي جدول من البنية الأصلية.
2. `npm run db:seed` — يضيف دور `strategic_project_lead` وصلاحياته
   (idempotent — لا يغيّر شيئاً قائماً).
3. لا شيء آخر: لا متغيرات بيئة جديدة، وحسابات القادة تُنشأ من لوحة المشرف.

## ١٢-٣ واجهات جديدة (كلها بحُرّاس الجلسة القائمين)

| الواجهة | من | ماذا |
|---|---|---|
| `GET/POST /api/projects/members` | اللجنة/المشرف | قائمة أعضاء المشاريع، وإنشاء/ربط عضو بالبريد وتثبيت قائده |
| `GET/PUT /api/projects/lead-identity` | لوحة المشرف | هوية كل حساب قائد (settings: `proj_lead:<userId>`) |
| `GET /api/projects/defs` و`/forms` | — | صارت مقصورة بحسب الدور: العضو مشاريعه، القائد مشاريع قيادته |

## ١٢-٣م تحديثات لاحقة في اليوم نفسه (ضمن هذه الحزمة)

- إسناد العضو: القائمة المسجلة أولاً وتبقى ظاهرة، والإضافة اليدوية لوحة
  تحتها — العضو غير المسجل يُدخل باسمه وبريده وهاتفه المتحرك (بتحقق الصيغة)
  ويُحفظ الهاتف على حسابه المنشأ
- إصلاح: الحساب الجديد «بدون جهة» لا يرث جهة المشرف المُنشئ، ومسح الجهة أو
  المسار من محرر المستخدم يُحفظ فعلاً
- إصلاح مشرف النظام: فتح لوحة المنسق من مبدّل الأدوار (كان يُقذف لنسخة
  الوزارة إن كانت جهته الوزارة)، ونسخة الوزارة تُفتح من مبدّل الجهات
- صفحة «من نحن»: زوال الخطوط العمودية في مسيرة التحول — الصور تتلاشى طبيعياً

## ١٢-٤ نتائج الفحص (قاعدة نظيفة)

الحزمة النهائية فُحصت كاملة على قاعدة بيانات نظيفة بُنيت بخطوات IT نفسها
(migrate deploy ثم seed ثم إنشاء الحسابات): **٨٩ فحصاً كلها ناجحة** —
٣٠ فحص واجهات (إدارة المستخدمين وأخطاؤها، الأدوار الأحد عشر بنطاقاتها،
المسارات المتعددة، وحدات الوزارة ونطاق الكتابة، الأعضاء والقادة، الحذف،
عدم انكسار الواجهات القائمة وترويسات الأمان)، و٢٩ فحص متصفح (التوجيه لكل
دور، معاينة المشرف، منتقي الأعضاء، صفحة القائد، دورة العضو كاملة)،
و٣٠ فحص استجابة على خمس صفحات وستة مقاسات بلا تمرير أفقي.


---

# ١٣) إصلاح ٣١ أغسطس (مساءً) — إسناد المسارات وتخصصا مسار العمليات

## ١٣-١ العلة والإصلاح

**العرض:** كان اسم المسار يُشتق من المسارات الثلاثة التي تُدار مدخلاتها داخل
المنصة، والرجوع لأولها عند عدم التطابق — فيظهر المسند إلى «بناء القدرات
والتدريب» أو «تقنيات الذكاء الاصطناعي والبيانات» على أنه «العمليات والدعم
المؤسسي». صار الاسم يُشتق من مسارات المشروع الخمسة كاملة، وتُعرض كل المسارات
المسندة للمستخدم لا الأول وحده.

**قاعدة البيانات:** جدول `streams` كان يحوي ثلاثة صفوف فقط، فيفشل إسناد
المسارين الآخرين. البذرة تنشئ الآن الخمسة (idempotent — لا تمسّ القائم).

**تخصصا مسار العمليات:** للمسار منسقان (عمليات الدعم المؤسسي، والعمليات
التخصصية). يُسند الحساب لأحدهما أو لكليهما من منتقٍ يظهر في محرر المستخدم
حين يكون مسار العمليات ضمن المسارات المحددة، ويظهر التخصص في القائمة بجوار
اسم المسار. يُخزَّن في جدول الإعدادات بمفتاح `ops_scope:<userId>` — بلا أي
تغيير في بنية قاعدة البيانات.

## ١٣-٢ المطلوب من IT

1. `npm run db:seed` — يضيف صفّي المسارين الناقصين (`capacity` و`tech`).
   **مطلوب**، وإلا بقي إسنادهما متعذراً.
2. لا ترحيل جديد ولا تغيير في البنية.
3. الحسابات التي أُسندت سابقاً لمسار خاطئ: تُصحَّح من محرر المستخدم في لوحة
   المشرف (اختيار المسار الصحيح ثم الحفظ).

## ١٣-٢م فريق عمل المسار المنتمي لوزارة شؤون مجلس الوزراء

انتماء الشخص للوزارة لا علاقة له بمساره: بنية الوزارة (جهات وقطاعات بدل
مسارات) تسري على من يعمل **داخل** الجهة — المنسق ونحوه — لا على فريق عمل
المسار الذي يعمل على مساره عبر الجهات كلها. لذلك:

| الدور | الجهة | المسارات | وحدات الوزارة |
|-------|-------|----------|----------------|
| فريق عمل المسار (ولو كانت جهته الوزارة) | اختيارية | **مطلوبة** | لا تظهر |
| منسق المسار وجهته الوزارة | مطلوبة | لا تظهر | تظهر (اختيارية) |
| منسق المسار وجهته اتحادية | مطلوبة | **مطلوبة** | لا تظهر |

كان اختيار الوزارة يُخفي منتقي المسارات لأي دور، فيتعذّر إسناد مسار لفريق
عمل مسار منتمٍ للوزارة — وقد صُحّح.

## ١٣-٣ نتائج الفحص

١٠٣ فحوص كلها ناجحة على القاعدة النظيفة نفسها: ٣٠ فحص واجهات، ٣٠ فحص
استجابة، و٤٣ فحص متصفح — منها سبعة للسيناريو المُبلَّغ عنه تحديداً (إسناد
لبناء القدرات يظهر صحيحاً، ومنتقي التخصصين، وقبول تخصص واحد أو كليهما،
وعرض مسارين معاً).

---

# ١٤ — تحديث ٣١ أغسطس (مساءً): نسبة الجهة على المدخلات + الحالة النطاقية

## ١٤-١ العلة المُبلَّغ عنها

مشرف يعاين دور المنسق لوزارة الطاقة والبنية التحتية رفع ملف Excel، وعند
العرض بدور «فريق عمل المسار» ظهرت المدخلات منسوبة لجهة أخرى.

**السبب:** المدخل كان يُحفظ **بلا حقل جهة**، وكل شاشة كانت تستنتج الجهة
من جلسة *المشاهد* لا من المدخل نفسه — فتظهر الجهة مختلفة باختلاف من يفتح
الشاشة.

## ١٤-٢ الإصلاح

1. **ختم الجهة عند الإنشاء**: كل مدخل (رفع Excel أو إضافة يدوية) يُختم
   لحظة إنشائه بالجهة الفاعلة ويُخزَّن بها؛ العرض يقرأ الجهة المخزونة.
2. **حارس «كل الجهات»**: مشرف يعاين المنسق على «كل الجهات» يُمنع من الحفظ
   حتى يختار جهة محددة من قائمة الجهات أعلى الصفحة.
3. **`POST /api/items`**: الأدوار الشاملة لم تعد تنشئ مدخلاً بلا `entityId`
   صريح (كان يسند جهة المشرف الشخصية بصمت — الآن ‎400).
4. **`/api/state` نطاقية**: كانت القراءة والكتابة مقصورتين على الأدوار
   الشاملة، فحسابات المنسقين ورؤساء المسارات الحقيقية كانت تعمل في متصفحها
   فقط ولا يصل عملها للخادم. الآن:
   - المنسق/ممثل الجهة يستلم ويحفظ مدخلات **جهته** (والمنسق ضمن مساراته) فقط.
   - رئيس المسار يستلم مساراته **بلا مسودات الجهات**، وتعديلاته (اعتماد/إعادة)
     تُدمج بالمعرّف ولا تحذف ما لم يصله؛ الحذف بالغياب لأدوار الجهة وحدها.
   - أي مدخل وارد خارج نطاق كاتبه يُتجاهل (جُرّب حقن مدخل مزوّر — يُرفض).
   - محتوى الموقع وإعدادات البرنامج لا يلمسها إلا الأدوار الشاملة.

## ١٤-٣ المطلوب من IT

1. نشر هذه الحزمة (لا ترحيل جديد ولا تغيير بنية ولا متغيرات بيئة).
2. **المدخلات التجريبية المرفوعة قبل هذا التحديث مخزونة بلا جهة** — تُحذف
   ويُعاد رفعها بعد النشر (وإلا بقيت تظهر بجهة المشاهد).
3. `npm run db:seed` إن لم يكن نُفِّذ بعد (صفّا المسارين — بند ١٣-٢).

## ١٤-٤ نتائج الفحص

٢٤ فحصاً جديداً للسيناريو المُبلَّغ عنه والحالة النطاقية (السيناريو الحرفي:
معاينة المنسق ← اختيار وزارة الطاقة ← رفع Excel ← العرض بدور الفريق يظهر
وزارة الطاقة على كل مدخل)، مع إعادة الرجرشن كاملاً: ٣٠ واجهات + ٣٠ استجابة
+ ٣٦ متصفح — **كلها ناجحة** (١٢٧ فحصاً).
