# AIGP — حزمة النشر على staging

**الفرع:** `it-after-changes-2` · **آخر إصدار:** 2026-08-12

هذه الحزمة هي نسخة كاملة من التطبيق (Next.js 14 + Prisma + PostgreSQL).
خطوات النشر بالتفصيل في `DEPLOYMENT.md`، وتفاصيل الأدوار والصلاحيات في
`docs/roles-pages-access.md`، وسجل التغييرات في
`docs/CHANGES-2026-08-11.md` (يتضمن ملحق 12 أغسطس).

---

## 1. ما الذي يلزم على مستوى قاعدة البيانات

**لا توجد ترحيلات جديدة في هذا الإصدار.** مجموعة الترحيلات الكاملة هي
`0001 … 0011` كما هي، وآخر ترحيلين أُضيفا في الإصدار السابق:

| الترحيل | المحتوى |
|---|---|
| `0010_service_catalog` | جدول `service_catalog` — دليل الخدمات الاتحادية |
| `0011_item_activities` | العمود `items.activities` (jsonb) — تفاصيل كل نشاط |

```bash
npx prisma migrate deploy      # 0001 → 0011
npm run db:seed                # بيانات مرجعية فقط (idempotent)
```

## 2. نتائج التحقق قبل التسليم

جرى التحقق على PostgreSQL 16 محلي وقاعدة بيانات فارغة:

| الفحص | النتيجة |
|---|---|
| `prisma validate` | ✅ صحيح |
| `prisma migrate deploy` على قاعدة جديدة | ✅ طُبّقت 0001 → 0011 |
| `npm run db:seed` | ✅ نجح (idempotent) |
| `service_catalog` بعد البذر | ✅ 1855 صفاً · 47 جهة · 288 خدمة رئيسية |
| `items.activities` | ✅ jsonb |
| `npm run build` (نسخة الخادم الكاملة) | ✅ نجح — جميع مسارات API مبنيّة |

## 3. أبرز ما تغيّر في هذا الإصدار

1. **دليل الخدمات مقيَّد بجهة المستخدم.** الخدمات الرئيسية والفرعية تتغير
   بتغير الجهة ولا تُعرض خدمات جهة أخرى إطلاقاً. التقييد مطبَّق في الخادم
   داخل `GET /api/svc-catalog` (وليس في الواجهة فقط): المنسق يحصل على دليل
   جهته حصراً، والأدوار العامة وحدها يمكنها الاستعلام عن جهة أخرى.
   الجهة التي لا صفوف لها تُدخل خدماتها يدوياً عبر «أخرى — إدخال يدوي».

   > **وزارة شؤون مجلس الوزراء غير مدرجة** في ملفَّي الخدمات المرفوعين، لذا
   > تظهر قائمتها فارغة. تحميل صفوفها في `service_catalog` يكفي — لا يلزم
   > أي تعديل برمجي.

2. **الإرسال للاعتماد على مستوى المدخل** في صفحة دفعات الإطلاق: زر «إرسال»
   لكل مدخل، أو تحديد مجموعة عبر مربعات الاختيار ثم «إرسال المحدد (ن)»،
   أو «إرسال الكل». نقطة النهاية:
   `POST /api/items/batch-drafts/:streamId/submit` بجسم `{ ids?: string[] }`.
   المدخلات الناقصة تُرفض كلٌّ على حدة دون تعطيل البقية.

3. **الرفض حالة مستقلة:** «تم الرفض» بلون أحمر في قوائم الحصر وجداول
   الدفعات، وسبب الرفض يظهر عند فتح التفاصيل، وخيار «تم الرفض» في معيار
   الحالة. القاعدة: `status_label = 'تم الرفض' WHEN returned_type = 'reject'`.

4. **دفعات الإطلاق:** المنسق ينقل النشاط بين الدفعات أو يزيله؛ ورئيس المسار
   يعتمد أو يعيد للتعديل أو يرفض من الجدول نفسه. القيمة الفارغة الصريحة في
   `activities[].exec_batch` تعني «أُزيل من دفعته» ولا ترث دفعة المدخل الأب.

5. **إرشادات استخدام المنصة:** جولة المنسق صارت 8 خطوات بإضافة خطوة مستقلة
   لرفع ملف Excel.

## 4. ما لم يتغيّر

- مسار الاعتماد (مسودة → قيد الاعتماد → معتمد/للتعديل) والمعتمِد
  (رئيس المسار ونائبه) — كما هو.
- الأدوار والصلاحيات والجلسات والمصادقة — كما هي.
- متغيرات البيئة وخطوات النشر — كما هي (`DEPLOYMENT.md`).
